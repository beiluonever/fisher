spring:
  application:
    name: fisher-gateway-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        group: GATE_GROUP
  profiles:
    active: dev
spring:
  application:
    name: fisher-back-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        group: BACK_GROUP
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true